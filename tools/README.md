# Training Tools

This directory is reserved for offline dictionary training tools.

Planned binaries:

- `train-zstd`
- `train-brotli`

These tools will be runnable in CI and will emit dictionary bytes plus metadata JSON.

